# build output
dist/
.astro/

# deps
node_modules/

# WordPress dump (build-time input only — DO NOT COMMIT, 2.77GB archive / 116MB sql)
_dump/

# scratch / crawl caches
.cache/
*.log

# os
.DS_Store

# 現実科学研究所 (RSI) materials — colocated under this folder but kept OUT of this
# PUBLIC repo: docs/ holds pre-launch confidential material (press releases, meeting
# transcripts) and apps/lp/ is its own live repo (StudioSaitama-LLC/dhw-lab).
/institute/
